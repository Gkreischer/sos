import { TotalClientsCountCardComponent } from './total-clients-count-card.component'

describe('TotalClientsCountCardComponent', () => {
  it('should mount', () => {
    cy.mount(TotalClientsCountCardComponent)
  })
})